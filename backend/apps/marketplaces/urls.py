from django.urls import path
from rest_framework.routers import DefaultRouter

router = DefaultRouter()
app_name = 'marketplaces'

urlpatterns = [
    # Marketplaces endpoints will be implemented in future user stories
] + router.urls